# SmartGym Risk Remediation Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve the current SmartGym AI project's confirmed defects and highest-risk latent issues found by static review plus Gradle verification.

**Architecture:** Android Java app using MVVM-ish ViewModels, Hilt, Firebase Auth/Firestore/Storage, Gemini SDK, Navigation Component, ViewBinding, and XML layouts. Fixes should preserve the existing Java/XML architecture while tightening security, data integrity, and user-facing flows.

**Tech Stack:** Java 17, Android SDK 35, Gradle/AGP 9.1.1, Hilt 2.59.2, Firebase BOM 33.14.0, Gemini AI SDK 0.9.0, AndroidX Navigation 2.9.0, JUnit/Robolectric/Mockito.

---

## Audit Prompt Used

Review this Android project as a senior Android/Firebase engineer. Produce an evidence-based audit, not generic advice. Inspect Gradle config, manifests, navigation, repositories, ViewModels, fragments, layouts, tests, and existing docs. Run available verification commands. For each issue, include severity, evidence file/line, likely impact, and a concrete remediation plan ordered by risk. Separate confirmed failures from potential risks and feature gaps. Avoid modifying production code during the audit; if a remediation plan is useful, save it under `docs/superpowers/plans/`.

## Verification Snapshot

- `.\gradlew.bat assembleDebug`: PASS.
- `.\gradlew.bat lintDebug`: PASS.
- `.\gradlew.bat testDebugUnitTest`: FAIL, 42 tests run, 1 failure.
- Failing test: `DashboardViewModelTest.formatGoalDisplay_positiveGoal_showsMinusPrefix`.
- Worktree was already dirty before this audit; do not revert unrelated edits.

---

## Phase 0: Stabilize The Baseline

### Task 1: Resolve the failing dashboard goal display test

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/dashboard/DashboardViewModel.java`
- Modify: `app/src/test/java/ntu/quy65132908/smartgym_ai/ui/dashboard/DashboardViewModelTest.java`

- [ ] Decide the product semantics for `goal`: target weight, weight delta, or free-text goal.
- [ ] If `goal` is target weight, rename `goalWeight`/`goalDisplay` labels to avoid showing a signed delta and update the test to expect `+6` only if the app really stores a delta.
- [ ] If `goal` is weight delta, make `parseGoalWeight()` preserve signs and change `formatGoalDisplay(6)` to return `−6` only if positive input means "kg to lose".
- [ ] Add tests for `"+6"`, `"-3"`, `"Giảm cân về 60kg"`, and `"Tăng cân lên 75kg"`.
- [ ] Run `.\gradlew.bat testDebugUnitTest`.
- [ ] Run `.\gradlew.bat assembleDebug`.

### Task 2: Commit or isolate the current uncommitted implementation

**Files:**
- Review: current `git status --short`.

- [ ] Confirm whether existing modified files are intended changes.
- [ ] Add missing tracked source `app/src/main/java/ntu/quy65132908/smartgym_ai/util/AvatarHelper.java`; it is referenced by fragments and currently untracked.
- [ ] Keep IDE-only changes out of commits unless intentionally required.
- [ ] Run `git diff --stat` before committing.

---

## Phase 1: Security And Configuration

### Task 3: Remove client-side Gemini secret exposure from release builds

**Files:**
- Modify: `app/build.gradle.kts`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/util/GeminiKeyProvider.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/GeminiRepository.java`

- [ ] Keep `local.properties` only for debug development and verify it remains untracked.
- [ ] Do not compile a real Gemini key into release APKs through `BuildConfig.GEMINI_API_KEY`.
- [ ] Move production AI calls behind a trusted backend endpoint or Firebase Callable Function; do not ship the raw key to clients.
- [ ] In debug builds, fail fast with a clear error if the key is blank.
- [ ] Add a unit test or small wrapper test for blank-key behavior.

### Task 4: Fix Google Sign-In client configuration and user-visible errors

**Files:**
- Modify: `app/google-services.json`
- Modify: `app/src/main/res/values/strings.xml`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/auth/LoginFragment.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/auth/AuthViewModel.java`

- [ ] Replace placeholder `default_web_client_id` with the OAuth web client generated by Firebase/Google Cloud.
- [ ] Regenerate `google-services.json` so `oauth_client` is not empty.
- [ ] Surface Credential Manager errors in `tvError` instead of logging only.
- [ ] Reuse a lifecycle-aware executor or shut it down after credential calls.
- [ ] Add tests for Google sign-in error mapping where feasible.

### Task 5: Lock down backup and sensitive personal data handling

**Files:**
- Modify: `app/src/main/AndroidManifest.xml`
- Modify: `app/src/main/res/xml/backup_rules.xml`
- Modify: `app/src/main/res/xml/data_extraction_rules.xml`

- [ ] Decide whether cloud backup should be disabled entirely for this fitness/photo app.
- [ ] If backup stays enabled, explicitly exclude auth/session caches and any local copies of personal/body photos.
- [ ] Remove unused `CAMERA` permission unless camera capture is actually implemented.
- [ ] Document privacy assumptions for Firestore and Storage data.

### Task 6: Add Firebase Security Rules to source control

**Files:**
- Create: `firestore.rules`
- Create: `storage.rules`
- Optional create/modify: `firebase.json`

- [ ] Add Firestore rules that restrict `users/{uid}` and subcollections to the matching authenticated user.
- [ ] Add community post rules that allow authenticated reads/writes with validation of `authorId`, content length, likes, and `likedBy`.
- [ ] Add Storage rules for `users/{uid}/before_photo.jpg` and `users/{uid}/after_photo.jpg` with size/content-type limits.
- [ ] Add emulator-based rule tests if Firebase emulator tooling is available.

---

## Phase 2: Core Product Flows

### Task 7: Replace the blank workout bottom-tab destination with a real workout list screen

**Files:**
- Create: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/workout/WorkoutListFragment.java`
- Create or modify: `app/src/main/res/layout/fragment_workout_list.xml`
- Modify: `app/src/main/res/navigation/nav_graph.xml`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/dashboard/DashboardFragment.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/dashboard/WeeklyPlanAdapter.java`

- [ ] Make bottom nav `nav_workout` point to a list screen, not `WorkoutDetailFragment`.
- [ ] Let "Xem tất cả" and "Tạo kế hoạch" navigate to meaningful screens.
- [ ] Make weekly plan rows clickable and pass `workoutId`, title, and duration to detail.
- [ ] Show an explicit empty state when no workout plan exists.
- [ ] Add navigation tests or Robolectric fragment tests if practical.

### Task 8: Persist generated workout plans as structured Firestore data

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/GeminiRepository.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/WorkoutRepository.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/analysis/AIAnalysisViewModel.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/model/Workout.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/model/Exercise.java`

- [ ] Change the Gemini prompt to request strict JSON matching `Workout` and `Exercise` fields.
- [ ] Parse and validate JSON before saving; reject malformed AI output with a clear message.
- [ ] Save workouts and nested exercises in a batch write.
- [ ] Refresh dashboard/workout list after save.
- [ ] Keep the raw Markdown response as optional display text, not the source of truth.

### Task 9: Make workout completion update workout-level stats

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/WorkoutRepository.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/workout/WorkoutViewModel.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/profile/ProfileViewModel.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/progress/ProgressViewModel.java`

- [ ] After exercise completion changes, compute whether all exercises are complete.
- [ ] Update parent workout `isCompleted` in the same logical operation.
- [ ] Avoid reloading the whole exercise list after every checkbox if local optimistic update is enough.
- [ ] Add tests proving profile/progress completed workout counts update after all exercises are done.

### Task 10: Add progress entry creation and validation

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/progress/ProgressFragment.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/progress/ProgressViewModel.java`
- Modify: `app/src/main/res/layout/fragment_progress.xml`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/ProgressRepository.java`

- [ ] Add UI to record weight, optional body fat, optional lean mass, and note.
- [ ] Validate physiologically reasonable ranges before saving.
- [ ] Use server timestamps where appropriate or normalize local dates to day buckets.
- [ ] Refresh chart and current weight after adding an entry.
- [ ] Add tests for `calculateWeightChange()` and `calculateStreak()` edge cases.

---

## Phase 3: Data Integrity And Runtime Robustness

### Task 11: Replace non-transactional community likes

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/CommunityRepository.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/community/PostAdapter.java`

- [ ] Use Firestore transaction or a subcollection model so likes cannot go negative or double-count under rapid taps.
- [ ] Disable the like button while a request is pending, or apply an optimistic update with rollback.
- [ ] Reflect liked/unliked state visually in the icon.
- [ ] Bind post time from `createdAt`; remove hardcoded time text.

### Task 12: Harden adapters against partial Firestore documents

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/dashboard/WeeklyPlanAdapter.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/workout/ExerciseAdapter.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/community/PostAdapter.java`

- [ ] Replace direct `.equals()` calls on nullable model fields with `Objects.equals()`.
- [ ] Include all rendered fields in `areContentsTheSame()`.
- [ ] Guard item click callbacks against missing IDs and show a recoverable error.
- [ ] Add unit tests for null title/name/content cases.

### Task 13: Fix async callback edge cases that leave loading stuck

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/AuthRepository.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/analysis/AIAnalysisViewModel.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/GeminiRepository.java`

- [ ] Ensure every auth success path calls either success or error even when Firebase returns a null user.
- [ ] Do not swallow Google profile creation failures silently; surface a non-blocking warning or retry.
- [ ] Replace `Executors.newSingleThreadExecutor()` per Gemini call with a singleton executor and shut it down in repository lifecycle if needed.
- [ ] Add cancellation/ignore-stale-result handling for ViewModels when users leave screens.

### Task 14: Improve date/time and server consistency

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/model/User.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/CommunityRepository.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/ProgressRepository.java`

- [ ] Prefer `FieldValue.serverTimestamp()` for Firestore-created records.
- [ ] Store progress entries by normalized local date plus timestamp to avoid streak errors around time zones.
- [ ] Update queries and model mapping to handle Firestore `Timestamp` safely.

---

## Phase 4: UX, Accessibility, And Maintainability

### Task 15: Remove hardcoded UI strings and demo metrics

**Files:**
- Modify: XML layouts under `app/src/main/res/layout/`
- Modify: `app/src/main/res/values/strings.xml`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/analysis/AIAnalysisFragment.java`

- [ ] Move visible strings from Java/XML into `strings.xml`.
- [ ] Replace AI analysis hardcoded body fat/lean mass/body type with real user/progress data or explicit empty states.
- [ ] Remove placeholder comments and placeholder OAuth values.

### Task 16: Add missing user-facing auth/account workflows

**Files:**
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/ui/auth/LoginFragment.java`
- Modify: `app/src/main/java/ntu/quy65132908/smartgym_ai/data/repository/AuthRepository.java`
- Modify: `app/src/main/res/layout/fragment_login.xml`

- [ ] Implement forgot-password flow for `tv_forgot_password`.
- [ ] Add email verification or document why it is not required.
- [ ] Disable all auth buttons while any auth operation is in flight.

### Task 17: Expand automated coverage beyond utility/ViewModel basics

**Files:**
- Add tests under `app/src/test/java/ntu/quy65132908/smartgym_ai/`
- Optional add instrumentation tests under `app/src/androidTest/`

- [ ] Keep current `assembleDebug`, `lintDebug`, and `testDebugUnitTest` as required checks.
- [ ] Add repository tests with fake callback implementations where Firebase can be mocked.
- [ ] Add ViewModel tests for progress, workout completion, community post creation, and AI error handling.
- [ ] Replace template `ExampleUnitTest` and `ExampleInstrumentedTest` with behavior-specific smoke tests.

### Task 18: Prepare release hygiene

**Files:**
- Modify: `README.md`
- Modify: `gradle.properties`
- Modify: `.gitignore`
- Optional modify: CI workflow if added later.

- [ ] Document setup steps for Firebase, Google OAuth, Gemini/backend config, and local test commands.
- [ ] Remove stale experimental Gradle flags once no longer needed.
- [ ] Add CI that runs `.\gradlew.bat lintDebug testDebugUnitTest assembleDebug` or Linux equivalent.
- [ ] Restrict Firebase API keys in Google Cloud by Android package and SHA certificate.

